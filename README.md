### Khanh Nguyen

Building **[Drop2Run](https://dropto.run)** — static hosting where you drag a folder
and get an HTTPS link in seconds. No git, no build step, no repository.

It also ships an [MCP server](https://github.com/Drop-to-run/drop2run-cli), so an
agent can publish the page it just wrote without leaving the conversation.

.NET · TypeScript · Cloudflare Workers · PostgreSQL
